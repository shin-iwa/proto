.contents.row
  .container
    .container-main
      = form_with(model: @article, local: true, class: "input-form") do |form|
        %h3.head
          編集する
        = render partial: "form", locals: { form: form }

    .comments
      %h4.line
      - if @novels
        - sum = 0
        - @novels.each do |novel|
          = sum +=1
          %p.comment
            %strong
              -# = link_to novel.user.nickname, "/users/#{novel.user_id}"


              = link_to novel.user.nickname, "/articles/#{@article.id}/novels/#{novel.id}"

              = novel.title
              
              -# = link_to "小説へ", article_novel_path(novel.id), method: :get
              ：
              
            = novel.id
            = novel.title
            - if user_signed_in? && current_user.id == @article.user_id
              %li.more_list__details
                = link_to '編集', edit_article_path(@article.id), method: :get
              %li.more_list__details
                = link_to '削除', article_path(@article.id), method: :delete

        -# = render partial: "novel", locals: { form: form }

    - if current_user
      .more-parts__edit
        この部分は次話作成フォームです。

        %div{:id => "novel-article-#{@article.id.to_s}"}
          -# = render 'comment_list', { post: post }
          -# = link_to time_ago_in_words(post.created_at).upcase + "前", post_path(post), |
            class: "light-color post-time no-text-decoration"                         |
          %hr/
          .row.actions{:id => "novel-form-article-#{@article.id.to_s}"}
            = form_with model: [@article, Novel.new], class: "w-100" do |f|
              = f.hidden_field :user_id, value: current_user.id
              = f.hidden_field :article_id, value: @article.id
              = f.text_field :body, class: "form-control novel-input border-0", placeholder: "コメント ...", autocomplete: :off
            -# = form.submit "投稿する" , class:"bt"


        -# = form_with(model: [@article, @novel], local: true, class:"inputArticle-form") do |form|
        -#   = form.text_field :title, placeholder: "タイトル", class:"yah"
        -#   = form.text_area :body, placeholder: "本文", rows: "10"
        -#   = form.submit "投稿する" , class:"bt"
        -# %ul.more_list
        -#   - if user_signed_in? && current_user.id == @article.user_id
        -#     %li.more_list__details
        -#       = link_to '編集', edit_article_path(@article.id), method: :get
        -#     %li.more_list__details
        -#       = link_to '削除', article_path(@article.id), method: :delete
        -# %hr
    - else
