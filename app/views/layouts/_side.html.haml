.side
  .side__top
    検索フォーム

  .side__center
    .side__center--box
      小説検索
      = form_with(url: search_articles_path, local: true, method: :get, class: "search-form") do |form|
        = form.text_field :keyword, placeholder: "小説を検索する", class: "search-input"
        = form.submit "検索", class: "search-btn"
    .side__center--box
      活動報告検索
      = form_with(url: search_tweets_path, local: true, method: :get, class: "search-form") do |form|
        = form.text_field :keyword, placeholder: "活動報告を検索する", class: "search-input"
        = form.submit "検索", class: "search-btn"
    .side__center--box
      ここには運営からのお知らせが入ります
    