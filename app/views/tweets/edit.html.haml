.contents.row
  .container
    .container-main
      = form_with(model: @tweet, local: true, class: "input-form") do |form|
        %h3.head
          編集する
        = render partial: "form", locals: { form: form }
