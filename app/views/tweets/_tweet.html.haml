%table.table
  %thead
    %tr
      %th.title
        %a.name-name{:href => tweet_path(tweet.id)}
          = tweet.title
      %th.author
        作者：
        %a.name-name{:href => "/users/#{tweet.user.id}"}
          = tweet.user.nickname
  %tbody
    %tr.secondRecord
      %td.secondRecord__category
        .text
          %p= simple_format(tweet.text.truncate(40, omission: '...')) 
      %td.secondRecord__link
        %p.created= tweet.created_at
        -# = link_to '詳細', tweet_path(tweet.id), method: :get
        -# = link_to '詳細', articles_path, method: :get
  -# %tfoot.lastRecord
  -#   %tr.lastRecord__base
  -#     %td.lastRecord__base--first
        -# .text
        -#   %p= simple_format(tweet.text.truncate(40, omission: '...')) 
      -# %td.lastRecord__base--second
      -#   %p.created= tweet.created_at

.more-parts-one

